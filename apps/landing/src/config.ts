/**
 * Лендінг стоїть окремо від платформи — на піддомені, зі своїм деплоєм, — але
 * веде туди ж: реєстрація (і згодом оплата) відбувається тут, а далі людина
 * їде на платформу вже з cookie сесії. Сесія спільна, бо бекенд один і той
 * самий, а cookie ставляться на батьківський домен (COOKIE_DOMAIN у бекенді).
 *
 * Адреса платформи задається під час збірки: локально це Vite на :5173,
 * у compose — nginx на :3000, на проді — app.<домен>.
 */
export const MAIN_URL = (import.meta.env.VITE_MAIN_URL ?? "http://localhost:5173").replace(/\/$/, "");

/** Куди вести після реєстрації: онбординг на платформі. */
export const AFTER_SIGNUP = `${MAIN_URL}/onboarding`;
