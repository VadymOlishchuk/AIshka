/**
 * Лендінг живе окремо (apps/landing, свій піддомен) і саме там реєструють
 * і платять. Платформа знає лише його адресу — щоб відправити туди тих,
 * у кого ще немає акаунта. Вшивається під час збірки.
 */
export const LANDING_URL = (import.meta.env.VITE_LANDING_URL ?? "http://localhost:5174").replace(/\/$/, "");
