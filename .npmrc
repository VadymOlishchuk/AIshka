# pnpm 11 перед кожним pnpm dev перевіряє залежності й падає через
# незатверджені build-скрипти. Скрипти нам не потрібні: Prisma 7 працює
# без Rust-движка, esbuild ставить бінарник як optional-залежність.
verify-deps-before-run=false
